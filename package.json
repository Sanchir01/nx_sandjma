{
  "name": "@go-workspace/source",
  "version": "0.0.0.1",
  "license": "MIT",
  "scripts": {
    "auth": "pnpm nx run sandjma_auth:serve",
    "category": "pnpm nx run sandjma_category:serve",
    "gateway": "pnpm nx run gateway:serve",
    "back": "pnpm nx run-many --target=serve --all --parallel"
  },
  "private": true,
  "dependencies": {
    "@nx-go/nx-go": "3.2.0"
  },
  "devDependencies": {
    "@nx/workspace": "19.6.0",
    "nx": "19.6.0"
  }
}
